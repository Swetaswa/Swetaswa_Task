import React from 'react';

function TicketCard({ ticket }) {
  return (
    <div className="card">
      {/* Render ticket details */}
      {/* Example: <p>{ticket.title}</p> */}
    </div>
  );
}

export default TicketCard;
